import 'package:flutter/material.dart';

class AllColors {
  AllColors._();

  // ── Core backgrounds ─────────────────────────────────────────────────────
  static const background    = Color(0xFF09090F);
  static const cardColor     = Color(0xFF111118);
  static const cardHigh      = Color(0xFF1A1A24);
  static const surfaceLight  = Color(0xFF22222E);

  // ── Primary ──────────────────────────────────────────────────────────────
  static const primaryColor  = Color(0xFFE50914);
  static const primaryDark   = Color(0xFFAA0A0F);
  static const primaryMuted  = Color(0x1AE50914);

  // ── Navigation ───────────────────────────────────────────────────────────
  static const navBackground = Color(0xFF0C0C14);
  static const bottomNavigatorBackgroundColor = navBackground;

  // ── Text ─────────────────────────────────────────────────────────────────
  static const white         = Color(0xFFF5F5F7);
  static const greyText      = Color(0xFF8E8E9A);
  static const textMuted     = Color(0xFF4E4E5E);

  // ── Status & Accents ─────────────────────────────────────────────────────
  static const yellow        = Color(0xFFFBBF24);
  static const green         = Color(0xFF10B981);
  static const blue          = Color(0xFF3B82F6);
  static const red           = Color(0xFFEF4444);
  static const purple        = Color(0xFFA855F7);

  // ── Legacy aliases ────────────────────────────────────────────────────────
  static const black         = Color(0xFF09090F);
  static const blueForText   = Color(0xFF788DA9);
  static const gray          = Color(0xFF7A7A76);
  static const lightGray     = Color(0xFFF2F2F2);
  static const borderGray    = Color(0xFFDEDEDF);
  static const secondary     = Color(0xFF10B981);
  static const surface       = Color(0xFFFFFFFF);
  static const textPrimary   = Color(0xFF1E293B);
  static const textSecondary = Color(0xFF64748B);
  static const textPlaceholder = Color(0xFF94A3B8);
  static const success       = Color(0xFF22C55E);
  static const error         = Color(0xFFEF4444);
  static const warning       = Color(0xFFF59E0B);
}
